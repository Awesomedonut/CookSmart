<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="#E64A19"/> <!-- Darker shade for pressed state -->
            <corners android:radius="8dp"/>
        </shape>
    </item>
    <!-- Default state -->
    <item>
        <shape>
            <solid android:color="#FF5722"/> <!-- Normal state color -->
            <corners android:radius="8dp"/>
        </shape>
    </item>
</selector>
